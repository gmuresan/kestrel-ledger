{
  "name": "aegentic-ledger",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx --yes wrangler deploy"
  },
  "devDependencies": {
    "@astrojs/rss": "4.0.12",
    "astro": "4.16.19"
  },
  "dependencies": {
    "marked": "18.0.5"
  }
}
